name: Build DLavie Shader

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate-and-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Validate DLavie pack
        run: python scripts/validate_pack.py
      - name: Generate preserved biome bindings
        run: python scripts/sync_vanilla_biomes.py --out biomes
      - name: Generate volumetric fog and light shafts
        run: python scripts/sync_vanilla_fogs.py --root .
      - name: Build mcpack
        run: python scripts/build_mcpack.py --skip-biome-sync --skip-fog-sync --output dist/DLavie-Shader-v0.1.3.mcpack
      - name: Upload mcpack artifact
        uses: actions/upload-artifact@v4
        with:
          name: DLavie-Shader-v0.1.3
          path: dist/DLavie-Shader-v0.1.3.mcpack
          if-no-files-found: error
